{
  "name": "mirror",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @mirror/api dev",
    "dev:web": "pnpm --filter @mirror/web dev",
    "dev:workers": "pnpm --filter @mirror/api workers",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}

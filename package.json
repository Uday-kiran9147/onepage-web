{
  "name": "readonepage",
  "private": true,
  "scripts": {
    "dev:api": "pnpm --filter @readonepage/api dev",
    "dev:web": "pnpm --filter @readonepage/web dev",
    "dev:workers": "pnpm --filter @readonepage/api workers",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18"
  }
}
